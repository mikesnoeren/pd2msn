{% set linkStyles = 'text-base font-medium text-white hover:text-slate-400' %}

<header class="bg-slate-900">
    <nav class="px-4 pb-6 mx-auto max-w-7xl sm:px-6 lg:px-8">
        <div class="flex justify-between items-center py-6 w-full border-b border-slate-500">
            <div class="flex items-center">
                {% include '_inc/helpers/logo.twig' %}
            </div>
            <div class="ml-10 space-x-4">
                <a href="{{ craft.entries.section('contact').one.url() }}" class="{{ buttonPrimary }}">
                    {{ craft.entries.section('contact').one.title }}
                </a>
                <a href="{{ craft.entries.section('blogOverview').one.url() }}" class="{{ buttonPrimaryInverted }}">
                    {{ craft.entries.section('blogOverview').one.title }}
                </a>
            </div>
        </div>
    </nav>
</header>